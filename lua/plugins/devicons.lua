return {
	"nvim-tree/nvim-web-devicons",
	lazy = true,
	opts = {
		color_icons = true, -- colored filetype icons (VSCode/Seti style)
		default = true, -- show a fallback icon for unknown filetypes
	},
}
